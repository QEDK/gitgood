[flake8]
exclude =
    # don't traverse git-related directory
    .git,
    .github,
    # don't traverse cached files
    __pycache__,
    # don't traverse venv files
    bin,
    lib,
    share,
    local
max-line-length = 99
